# Bearer token from https://yce.makeupar.com/api-console/en/api-keys/
YOUCAM_API_KEY=

# Allowed CORS origin (your Cloudflare Pages URL). Use * only for local dev.
ALLOWED_ORIGIN=http://localhost:5173

# Optional override (defaults to YouCam US endpoint)
# YOUCAM_BASE=https://yce-api-01.makeupar.com
